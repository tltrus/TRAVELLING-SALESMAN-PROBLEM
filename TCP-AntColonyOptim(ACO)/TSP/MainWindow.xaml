<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="Ant algorithm / Microsoft Test Run" Width="972" Height="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="199*"></ColumnDefinition>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <local:DrawingVisualClass  x:Name="g" Margin="5,5,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="580" Background="White" Height="385"/>
        </Grid>

        <Grid Grid.Column="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
                <Button x:Name="btnUpdate" Content="UPDATE" Margin="5" Padding="10" Click="btnUpdate_Click" Grid.Row="0"/>
                <Label x:Name="lbT" Content="Time:" Grid.Row="1"/>
                <RichTextBox x:Name="rtbConsole" Margin="5" FontSize="9" Foreground="#FF4C4C4C" 
                 VerticalScrollBarVisibility="Visible" Grid.Row="2" VerticalAlignment="Stretch">
                    <RichTextBox.Resources>
                        <Style TargetType="{x:Type Paragraph}">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </RichTextBox.Resources>
                </RichTextBox>
        </Grid>
    </Grid>
</Window>
